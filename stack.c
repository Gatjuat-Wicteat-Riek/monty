#include "monty.h"
/**
 * f_stack -This wil  print the top elements
 * present in the stack.
 * @head: This is the stack head present.
 * @count: This is the line_number present.
 * Return: nothing to  return
*/
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lf = 0;
}
